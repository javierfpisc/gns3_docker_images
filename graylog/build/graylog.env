#Lista de paquetes
PACKS="docker-compose"

#Comandos a ejecutar al crear la imagen separados por "," (Actualizamos docker-compose a una versión reciente)
COMMANDS="wget https://github.com/docker/compose/releases/download/v2.12.2/docker-compose-$(uname -s)-$(uname -m) -O /usr/bin/docker-compose,chmod +x /usr/bin/docker-compose"

