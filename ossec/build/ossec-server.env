#Lista de paquetes
PACKS="wget iproute2 vim iputils-ping net-tools"

#Comandos a ejecutar al crear la imagen separados por ","
COMMANDS="DEBIAN_FRONTEND=noninteractive wget -q -O - https://updates.atomicorp.com/installers/atomic | bash && apt update && apt install -y ossec-hids-server"

