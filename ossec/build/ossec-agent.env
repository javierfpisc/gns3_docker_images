#Lista de paquetes
PACKS="wget vim iproute2 iputils-ping net-tools"

#Comandos a ejecutar al crear la imagen separados por ","
COMMANDS="export DEBIAN_FRONTEND=noninteractive,wget -q -O - https://updates.atomicorp.com/installers/atomic | bash,apt update && apt install -y ossec-hids-agent"

