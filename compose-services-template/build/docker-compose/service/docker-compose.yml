version: "3.8"

services:
  debian:
    image: "debian:latest"
    hostname: "service"
    volumes:
      - "/tmp:/tmp"
    restart: "on-failure"
#    depends_on:
#     service1:
#     service2:
# etc...
