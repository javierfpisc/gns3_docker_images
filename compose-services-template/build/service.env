#Lista de paquetes
PACKS="docker-compose"

#Versión de docker-compose a ejecutar
COMPOSEV=2.12.2

#Comandos a ejecutar al crear la imagen separados por "," (Actualizamos docker-compose a una versión reciente)
COMMANDS="wget \"https://github.com/docker/compose/releases/download/v${COMPOSEV}/docker-compose-linux-$(uname -m)\" -O /usr/bin/docker-compose,chmod +x /usr/bin/docker-compose"


