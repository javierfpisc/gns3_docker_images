#Lista de paquetes
PACKS="bridge-utils"

#Comandos a ejecutar al crear la imagen separados por ","
COMMANDS="DEBIAN_FRONTEND=noninteractive apt install -y suricata,cd /tmp && wget https://artifacts.elastic.co/downloads/beats/filebeat/filebeat-8.1.1-amd64.deb && apt install -y /tmp/filebeat-8.1.1-amd64.deb"

