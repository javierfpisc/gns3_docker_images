services:
#Services
 apache_mysql:
  image: javierfpisc/brs_wordpress:apache_mysql
  build:
    context: build
    args:
     ENVPATH: apache_mysql.env
 apache:
  build:
    context: build
    args:
     ENVPATH: apache.env

