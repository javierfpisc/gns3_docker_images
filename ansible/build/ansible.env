#Lista de paquetes
PACKS="vim curl iproute2 iputils-ping nano wget procps git ansible"

#Comandos a ejecutar al crear la imagen separados por ","
COMMANDS="mkdir -p /etc/ansible,touch /etc/ansible/hosts,echo [defaults] >> /etc/ansible/ansible.cfg,echo inventory=/etc/ansible/hosts >> /etc/ansible/ansible.cfg"

